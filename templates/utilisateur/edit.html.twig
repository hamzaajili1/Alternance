{% extends 'base.html.twig' %}

{% block content %}
    <h1>Modifier l'utilisateur {{ utilisateur.nom }}</h1>
<style> 
    
        #userform button {  
         border: 1px solid;
         padding: 10px; 
         font-size:16px; 
         box-shadow: 2px 5px #888888;
      } 
   </style> 
   <div id = "userform">
    {{ form_start(form) }}
    {{ form_widget(form) }}

    </br><button type="submit" class="btn btn-primary">Modifier</button>
      
      <a href="{{ path('utilisateur_index') }}">
    <button type="button">Retour vers la liste des chantiers</button>
    </a>
    {{ form_end(form) }}

   
    </div>
{% endblock %}
